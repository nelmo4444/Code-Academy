#include <stdio.h>

int iz4istiBitSled(int num, int pos) {
    int maska = ~(((1 << (pos+1)) - 1));
    return num & maska;
}

int main() {
   int n = 255;
   printf("%d\n", iz4istiBitSled(n,5));
   return 0;
}